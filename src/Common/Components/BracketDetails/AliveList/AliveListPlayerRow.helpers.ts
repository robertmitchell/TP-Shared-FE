import type { BracketResults, EventData } from '@/Common/Common.types'

/**
 * Calculates the estimated due based on first places and second places
 */
export const calculateEstimatedDue = (
  bIndex: number,
  eventData: EventData,
  results: BracketResults,
) => {
  return 0
}
