import type { SetState } from '@/Common/Common.types'

/**
 * Sends password reset email to the user's email
 **/
export const sendResetEmail = (
  email: string,
  setSuccess: SetState<boolean>,
) => {
  console.log('This logic has been removed')
}
