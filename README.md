# RUNNING THE APP

Launch the app via: `npm run dev`

> Local: http://localhost:3000/
> Network: use `--host` to expose `npm run dev-host`
> HTTPS Flag: `npm run dev-host -- --https`

If you need to test backend functions and get CORS errors do this:

- Open `localhost.localdomain:3000` in the browser
